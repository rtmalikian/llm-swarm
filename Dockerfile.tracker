FROM python:3.10-slim

WORKDIR /app

# Tracker doesn't need llama-cpp-python, so we install only its core dependencies
RUN pip install --no-cache-dir fastapi uvicorn pydantic

COPY tracker.py .

EXPOSE 12345
CMD ["python", "tracker.py"]
